[build]
builder = "DOCKERFILE"
dockerfilePath = "deploy/railway/minio-init/Dockerfile"
watchPatterns = ["deploy/railway/minio-init/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
